<thead>
    <th class="column column1"><%= references_column_header('title') %></th>
    <th class="column column2"><%= references_column_header('type') %></th>
    <th class="column column3"><%= references_column_header('authors') %></th>
    <th class="column column4"><%= references_column_header('year') %></th>
    <th class="column column5"><%= references_column_header('files') %></th>
    <th class="column column6">Labels</th>
    <th class="column select-column"></th>
</thead>
<tbody id="reference-table-body">
    <%= render(@references) %>
</tbody>